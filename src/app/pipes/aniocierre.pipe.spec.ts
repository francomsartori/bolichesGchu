import { AniocierrePipe } from './aniocierre.pipe';

describe('AniocierrePipe', () => {
  it('create an instance', () => {
    const pipe = new AniocierrePipe();
    expect(pipe).toBeTruthy();
  });
});
